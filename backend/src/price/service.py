import requests


def fetch_necessities_prices(category: str, commodity: str) -> dict:
    return requests.get(
        "https://opendata.ey.gov.tw/api/ConsumerProtection/NecessitiesPrice",
        params={"CategoryName": category, "Name": commodity},
    ).json()